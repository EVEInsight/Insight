;this file must be renamed to config.ini before running the application
[database_used]
database_type = sqlite
;specify the type of database to use. Options are either sqlite or mysql. Change database_type from sqlite mysql to make use of a mysql database
[sqlite_database]
filename = Database.db
[mysql_database]
host = 127.0.0.1
port = 3306
db = db
user = root
pass = pass

[discord]
token =
;required - Create a new Discord app at https://discordapp.com/developers/applications/me and set token to your App's token
[ccp_developer]
client_id =
secret_key =
;optional - Create a new CCP Application at https://developers.eveonline.com/applications/create with the following scopes: esi-alliances.read_contacts.v1, esi-corporations.read_standings.v1, esi-characters.read_standings.v1
;The bot will still function without a CCP app but automatic syncing of ignore lists to capRadar feeds will be unavailable